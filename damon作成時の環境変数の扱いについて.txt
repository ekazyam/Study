■daemon作成時の環境変数の扱いについて
実行ユーザはrootであるが、必ずしもrootユーザが実行する環境変数が全て設定されている
とは限らない。
bashrc等は読み込まれておらず、デバッグ時に想定していた値が取得できない可能性がある。
日本語が扱えない等のトラブルを回避する為、以下を記載する。
LANG=ja_JP.UTF-8

# Export Parameter.
export LANG